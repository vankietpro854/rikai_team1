<p id="notice"><%= notice %></p>

<h1>Reports</h1>


<%= link_to 'Tạo report', new_report_path %>
<hr>
<table class="table"> 
  <thead class="thead-dark">
    <tr>
      <th>#</th>
      <th>Title</th>
      <th>User</th>
      <th>Content</th>
      <th class="text-center">Edit</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reports.each do |report| %>
      <% if current_user.id == report.user.id %>
      <tr>
        <td><%= report.id %></td>
        <td><%= report.title %></td>
        <td><%= report.user.name if report.user %></td>
        <td><%= raw(report.content) %></td>
        <td class="text-center">
          <%= link_to 'Show', report %> |
          <% if report.created_at.beginning_of_day == DateTime.now.to_date %>
          <%= link_to 'Edit', edit_report_path(report) %> |
          <% end %>
          <%= link_to 'Destroy', report, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

